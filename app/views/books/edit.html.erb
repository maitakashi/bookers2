<p id="notice"></p>
<div class="profile-container">
  <% if @books.errors.any? %>
      <%= @books.errors.count %>error prohibited this obj from being saved:
      <% @books.errors.full_messages.each do |message| %>
      <%= message %>
      <% end %>
      <% end %>
  <div class="row">
  <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
    <h1>Editing Book</h1>
    <%= form_with model:@books, local: true do |f| %>
    <h4>Title</h4>
    <%= f.text_field :title %>
    <h4>Opinion</h4>
    <%= f.text_area :body %></br>
    <%= f.submit 'Update Book' %>
    <% end %>
    <%= link_to "Show", book_path(@books) %>|<%= link_to "Back",books_path %>
   </div>
   </div>
   </div>